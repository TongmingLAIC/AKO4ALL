# Core dependencies for kernel-opt-agent
# Python 3.12 recommended

# PyTorch with CUDA 12.8+
torch>=2.9.1

# GPU kernel languages
triton>=3.5.0

# FlashInfer-Bench SDK (install from source or GitHub)
# flashinfer-bench @ git+https://github.com/flashinfer-ai/flashinfer-bench.git@main

# Profiling (CUPTI requires CUDA 13.0+ driver; falls back to CUDA events on older drivers)
cupti-python

# Modal backend (optional, for cloud benchmarking)
modal>=1.3.0

# Utilities
numpy>=1.24.0
torch-c-dlpack-ext

# TVM FFI for CUDA/C++ bindings
tvm-ffi-ct
